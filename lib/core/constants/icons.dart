import 'package:flutter/material.dart';
import 'package:imagine_notes/core/constants/palette.dart';

class AppIcons {
  static const Icon note = Icon(Icons.note, color: Palette.white, size: 35);

  static const Icon logout = Icon(Icons.logout);

  static const Icon clear = Icon(Icons.clear);

  static const Icon delete = Icon(Icons.delete, color: Palette.error);

  static const Icon edit = Icon(Icons.edit);

  static const Icon search = Icon(Icons.search);

  static const Icon travel = Icon(Icons.travel_explore, size: 30);

  static const Icon more = Icon(Icons.add, color: Palette.white, size: 24);
}
